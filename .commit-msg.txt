双轨下线收尾：阶段 1-4 全量落地 + A6/G5 退役 + 配置与鉴权终局

阶段 1-2（无风险删除 + 差分 harness + 四切流）：
- 无风险删除批次与差分收口：D3/D4/D1/D2 依次"造 harness → 断言等价 → 删输家与 flag"
- 修复 CouponRule 丢券潜伏缺陷（UserId 门槛与映射冲突，flag 开启即静默丢券）
- Saga 状态机与支付后 PM 两个不可用原型整体退役（证据驱动裁决：事件无发布方/命令无消费者）
- 涉及服务 19 → 16，特性开关 6 → 0

阶段 3（结构性退役，同一维护窗口语义）：
- A1/A2：PointsMembership/ReviewAfterSales 退役（16 工程 + compose/Helm/CI/KV/slnx/SQL 全套物料），
  Order 与网关调用面改指 Points/Review/AfterSales 新 BC
- A6+G5：UserAuth 退役 + RS256-only —— Identity 暴露 JWKS/OIDC 发现端点，
  全服务 JwtBearer 改发现文档验签；删 HS256 共享密钥/GatewayHeader 透传/Auth:Mode；
  网关 9 条冲突路由切换；Notification 改指 Identity

配置管理：
- kv-seed 补全（Jwt__DiscoveryUrl/ServiceUrls__IdentityApi/敏感键占位，删 Jwt__SecretKey）
- ValidateSensitiveConfig/TestWebHostHelper 同步（修 P0 启动阻断）
- 新增 scripts/validate-config-keys.ps1 三方键一致性校验（appsettings×17/kv-seed×35/敏感键×13 首跑通过）

鉴权与密钥（P1/P2 改进）：
- JWKS 多钥发布（JwtSigning:PreviousKeyIds）+ 四步轮换 runbook（deploy/docs/jwt-key-rotation-runbook.md）
- 消费方刷新调优（AutomaticRefreshInterval=1h/RefreshInterval=30s）+ kid 未命中即时 RequestRefresh
- JWKS 端点输出缓存 + RequireHttpsMetadata 生产门禁（移入启动校验器，IHostEnvironment 判定）

阶段 4（读模型终局）：
- 双写点清点：命令路径零 ES 双写（E3 无可删项）；E1-Product 核实达成（DEC-9）
- E2-SellerShop 收口：Dashboard 固定走 ES + 零值兜底，删 DashboardFeatureOptions/双发对比/SQL 累计查询
- 新增 scripts/rebuild-es.ps1（SystemAdmin 编排触发 + 进度轮询）

本机 Docker E2E 环境（阶段 5 前置）：
- 修复 compose depends_on 损伤；inventory-api 镜像构建与容器化调试（gRPC h2c 端点、Consul KV 键名、敏感键种子）
- 新增 scripts/verify/{InventoryE2E,staging-inventory.ps1}（gRPC 预占/确认/释放/查询 + 幂等 + 超卖拒绝 + SQL 台账断言）

验证：全量构建 0 Error；核心套件全绿（Infra 581、Gateway 276、Order.App 50、
Identity.App 131、SystemAdmin 61、SellerShop 37/9、Points 49、Promotion 84、Cart 88 等）
