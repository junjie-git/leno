# Leno 双轨情况检测报告

> 检测时间：2026-09-18
> 检测范围：`src/`（19 个 BC + BuildingBlocks）、`docker-compose.yml`、`deploy/`（Helm + Consul KV）、各 BC `appsettings*.json`
> 检测方式：静态代码/配置阅读 + 全量解决方案编译验证（`dotnet build Leno.slnx` → 0 Error）
> 关联前置工作：Outbox 发布器未注册缺陷修复（见第 1 节）

---

## 1. 修复执行摘要（Outbox 发布器）

### 1.1 缺陷重述
域事件经 `SaveChangesWithOutboxAsync` 与业务数据同事务写入 `outbox_messages`（写路径完整），
但**没有任何 BC 注册 Outbox 后台发布器**（读路径缺失），消息将永久停留在 `Pending`，
所有跨 BC 异步链路静默失效。

### 1.2 修复过程中暴露的第二层缺陷
`ShardedOutboxPublisher<TDbContext>` 以 `AddHostedService`（singleton）注册，
却在构造函数注入 `IEventBus`（`services.AddScoped<IEventBus, RabbitMqEventBus>()`）。
这是 **captive dependency（捕获依赖）**：在 `ValidateScopes` 开启时（Development 环境默认开启）
会导致宿主启动直接失败。因此"补一行注册"的朴素修复会立刻让本地开发环境崩溃 ——
修复必须同时打断该依赖。

### 1.3 实际改动

| # | 文件 | 改动 |
|---|------|------|
| 1 | `src/BuildingBlocks/Leno.Infrastructure.Persistence/Outbox/ShardedOutboxPublisher.cs` | 构造函数移除 `IEventBus` 参数；改为在每条消息的独立作用域内解析 `IEventBus`（`PublishSingleByIdAsync` 中与 `TDbContext` 同作用域），`PublishSingleAsync` 相应新增 `eventBus` 形参。补充 XML 文档说明为何不得构造注入。 |
| 2 | `src/BuildingBlocks/Leno.Infrastructure/Dependencies/WebApplicationExtensions.cs` | 在 `AddLenoApi<TDbContext>` 组合根统一调用 `services.AddShardedOutboxPublisher<TDbContext>(configuration)`，一处覆盖全部 19 个 BC；新增 `using Leno.Infrastructure.Outbox;`，同步更新方法 XML 摘要。 |
| 3 | `src/BuildingBlocks/Leno.Infrastructure.Tests/Outbox/ShardedOutboxPublisherTests.cs` | `LinqBackedShardedOutboxPublisher` 子类与 `CreateSut` 同步去掉 `eventBus` 形参；改为在 `ServiceCollection` 注册 `IEventBus` mock（更贴近真实解析路径）；2 处配置校验测试同步调整。 |

### 1.4 验证结果

- `dotnet build Leno.slnx` → **Build succeeded，0 Error**（763 Warning 全部为既有 `LENO_PM_BC_SPLIT` 等历史告警，与本次改动无关）
- `dotnet test --filter "FullyQualifiedName~Outbox"` → **63/63 Passed**
- 未引入新告警；受影响程序集 `Leno.Infrastructure.Persistence` / `Leno.Infrastructure` / `Leno.Infrastructure.Tests` 均 0 warning

### 1.5 部署待办（修复后仍需运维侧动作）
- 单实例：无需配置，默认 `ShardCount=1 / ShardId=0`
- 多实例：需为每个实例注入 `OUTBOX__SHARDING__SHARD_ID` 与 `OUTBOX__SHARDING__SHARD_COUNT`（或写入 Consul KV `leno/config/Outbox__Sharding__*`）
- 建议在 Helm `values.yaml` 增加 `Outbox:Sharding` 段并在 `kv-seed.json` 登记，避免多副本部署时所有副本抢同一分片

---

## 2. 双轨检测总览

共检测到 **27 项**双轨，按性质分为七类：

| 类别 | 项数 | 性质 | 是否需处理 |
|------|------|------|-----------|
| A. BC 拆分 / 服务部署面 | 5 | 遗留 | **是（4 项）** |
| B. 事件与消息机制 | 4 | 遗留（含死代码） | **是（3 项）** |
| C. 跨 BC 通信通道 | 3 | 有意保留（灰度期） | **是（2 项）** |
| D. 业务路径灰度开关 | 4 | 有意保留（灰度期） | **是（4 项）** |
| E. 查询 / 读模型 | 3 | 遗留 | **是（3 项）** |
| F. 持久化 / 基础设施 | 5 | 混合 | **是（3 项）** |
| G. 鉴权 / 配置 | 3 | 混合 | **是（2 项）** |

---

## 3. 详细清单

### A. BC 拆分与部署面双轨

#### A1 — PointsMembership BC 拆分未完成（旧 BC 仍在承担全部线上流量）
- **位置**
  - `src/Services/PointsMembership/**`（整个旧 BC；8 个类型带 `[Obsolete(..., DiagnosticId = "LENO_PM_BC_SPLIT")]`，双轨期声明 8 周）
  - `docker-compose.yml:346-349`：`points-api` 容器**实际构建** `Leno.PointsMembership.Api/Dockerfile`
  - `deploy/helm/leno/values.yaml:193-208`：`pointsmembership` 服务组
  - 新 BC `src/Services/Points/**`、`src/Services/Membership/**` 已实现但无容器
- **原因**：阶段四步骤 4.9 的 BC 拆分，采用"新老并存 + 灰度切流"策略
- **影响**：新 BC 代码完备但零流量；旧 BC 持续演进会与新 BC 逻辑漂移；编译期持续产生大量 `LENO_PM_BC_SPLIT` 告警
- **是否需处理**：**是** —— 声明的 8 周双轨期已过，应决策切流或明确延期

#### A2 — Review / AfterSales 拆分未部署
- **位置**：`docker-compose.yml:376-379`（`review-aftersales-api` 构建 `Leno.ReviewAfterSales.Api`）；`Leno.Review.Api`、`Leno.AfterSales.Api` 无 Dockerfile 引用、无 compose 服务
- **原因**：同上，BC 拆分双轨
- **影响**：`Review` 与 `AfterSales` 两个 BC 具备独立 `DbContext`、`IOrderStatusProvider`、`AfterSalesEligibilityChecker`，但仅在单体内运行
- **是否需处理**：**是**（确认是否计划独立部署，否则应考虑将代码合并回单一 BC 以消除分裂）

#### A3 — 8 个 BC 已实现未部署
- **位置**：以下 `.Api` 项目均无容器：`Leno.Identity.Api`、`Leno.AccessControl.Api`、`Leno.UserCenter.Api`、`Leno.Inventory.Api`、`Leno.Points.Api`、`Leno.Membership.Api`、`Leno.Review.Api`、`Leno.AfterSales.Api`
  - 但 `deploy/consul/kv-seed.json:5-23` 已为全部 19 个 BC 准备 `ConnectionStrings__*Db`
  - `docker-compose.yml` / Helm `values.yaml`（声明"12 个服务 = 11 BC + 1 网关"）只部署 11 个
- **原因**：渐进式交付，部署面落后于代码面
- **影响**：`AccessControl` / `Identity` 的 `appsettings.json` 已开 `UseGrpc: true`，暗示预期上线，但实际无进程；Order 的库存预占走 Redis 实现，`Inventory` BC 未上线形成"两套库存实现"
- **是否需处理**：**是**（部署就绪度缺口，建议列入 GO/NO-GO 清单）

#### A4 — 19 个 BC 数据库 vs 11 个已部署服务
- **位置**：`deploy/consul/kv-seed.json` 19 条 `ConnectionStrings__*Db`；`deploy/helm/leno/files/migrations/` 仅 12 个 `*-initial.sql`
- **原因**：随 A3
- **影响**：8 个 DB 的连接串已下发但无消费者；迁移脚本覆盖不全
- **是否需处理**：**是**（随 A3）

#### A5 — Helm 与 docker-compose 服务清单不一致
- **位置**
  - Compose 端口：`points-api:5157`、`review-aftersales-api:5158`
  - Helm 端口：`pointsmembership:5157`、`reviewaftersales:5156`、`payment:5158`
- **原因**：两套编排独立演进
- **影响**：端口映射漂移（Payment 在 compose 为容器内 8080 / Helm 为 5158），跨环境排障易混淆
- **是否需处理**：**是**（低优先级，建议统一端口矩阵）

---

### B. 事件与消息机制双轨

#### B1 — Outbox 发布器双实现
- **位置**
  - 旧：`src/BuildingBlocks/Leno.Infrastructure.Persistence/Outbox/OutboxPublisher.cs`（`[Obsolete]`，"双轨期 4 周后移除"）
  - 新：同目录 `ShardedOutboxPublisher.cs`
  - 测试：`Leno.Infrastructure.Tests/Outbox/OutboxPublisherTests.cs`、`OutboxPublisherConditionalUpdateTests.cs` 仍针对旧类
- **原因**：4.4 分片发布器替换单实例发布器
- **影响**：旧类从未被注册（本次修复也未注册它），属**纯死代码**；且其构造函数存在与新版相同的 captive dependency（若有人误注册同样会崩）
- **是否需处理**：**是** —— 删除 `OutboxPublisher.cs` 及两个测试文件，彻底消除歧义

#### B2 — 域事件"双身份"回退分支（死代码）
- **位置**：`OutboxDbContextExtensions.cs` 两处重载中的
  `if (integrationEvent is null && domainEvent is IIntegrationEvent legacyEvent)` 回退分支
- **原因**：双发期兼容 —— 旧模式下领域事件直接实现 `IIntegrationEvent`
- **影响**：经全量检索，`src/Services` 下**不存在任何实现 `IIntegrationEvent` 的领域事件**；15 个 BC 已各有 `*IntegrationEventMapper`，4 个 BC 使用 `NullIntegrationEventMapper`。该分支**不可达 = 死代码分支**
- **是否需处理**：**是** —— 移除回退分支与 `NullIntegrationEventMapper` 兜底说明，简化 `SaveChangesWithOutboxAsync`

#### B3 — 直接发布 vs Outbox 发布
- **位置**
  - `RabbitMqEventBus`（`IEventBus`）仍对所有 BC 注册且为 scoped
  - 文档明令禁止直接 `PublishAsync`（`docs/handbook/05-cross-bc-communication.md:109`）
  - 实际仍直接使用 `IBus` 的位置：`OrderSagaOrchestrator`（`ScheduleSend` 延迟消息，`queue:order-timeout` 等）—— 属**合法例外**
- **原因**：Outbox 迁移期的双通道
- **影响**：延迟消息经 `IBus` 直发不过 Outbox，若发送后进程崩溃则延迟消息丢失（`OrderTimeoutDelayMessageConsumer` 的兜底依赖状态二次校验）
- **是否需处理**：**待确认** —— 需确认是否接受延迟消息的"至多一次"语义，或改走持久化调度

#### B4 — 延迟消息 / 定时消息双调度器
- **位置**：`OrderSagaOrchestrator` 使用 `CreateMessageScheduler().ScheduleSend()`（MassTransit 调度器，依赖 RabbitMQ delayed-message 插件或 Quartz）；`OrderTimeoutDelayMessageConsumer`、`AfterSalesWindowConsumer` 消费
- **原因**：延迟投递实现方式可选
- **影响**：需确认 broker 侧是否启用 `rabbitmq_delayed_message_exchange` 插件，否则调度器不可用
- **是否需处理**：**待确认**（运维配置核对项）

---

### C. 跨 BC 通信通道双轨（M4 方案）

#### C1 — 防腐层 HTTP vs gRPC 双轨（19 个 BC 级开关）
- **位置**
  - 开关：`AntiCorruption:UseGrpc`（各 BC `appsettings.json`），运行时由 `ConsulConfigWatcher` 监听 `leno/anticorruption/use-grpc/{bc}` 热更新
  - 双实现：HTTP 版（`*AntiCorruptionService` / `Http*Provider`）+ gRPC 版（`Services/Grpc/Grpc*Client.cs`）+ 调度适配器（`*DispatcherAdapter.cs`）+ 熔断器 `CircuitBreakerState`
  - 涉及 BC：Order（Product/Promotion/Points 三路）、Cart（Product 快照 / 价格）、Review、AfterSales、Notification、SellerShop、ReviewAfterSales 等
- **原因**：M4 渐进式 gRPC 迁移，HTTP 保留为降级备份
- **影响（重要配置冲突）**：`appsettings.json` 中 **AccessControl 与 Identity 声明 `UseGrpc: true`**，
  而 `deploy/consul/kv-seed.json:47-57` 为**全部 19 个 BC** 播种 `false`，
  且 Consul KV 位于配置链尾（优先级高于 `appsettings.json`）。
  → **运行时实际生效值为 HTTP**，与 appsettings 声明相反，运维排查极易误判
- **是否需处理**：**是** —— 统一 appsettings 与 KV 声明；建议将 KV 播种值改为"与环境实际一致"，或移除 appsettings 中的重复声明（单一事实来源）

#### C2 — 跨 BC 内部路由双前缀 `internal/...` vs `internal/v1/...`
- **位置**（均已标注下线日期，多数已过期）
  | 文件 | 行 | 旧路由 | 声明下线 |
  |------|----|--------|---------|
  | `Order/…/InternalOrdersController.cs` | 23 | `internal/orders/{orderId}/status` | 2026-08-15 **已过期** |
  | `Product/…/InternalProductsController.cs` | 24, 41 | `internal/products/skus/...` | 2026-08-15 **已过期** |
  | `Promotion/…/InternalPromotionsController.cs` | 29, 43 | `internal/promotions/calculate`、`lock-coupon` | 2026-09-15 **已过期** |
  | `PointsMembership/…/InternalPointsController.cs` | 24,35,46,57 | `internal/points/*` | 2026-08-01 **已过期** |
  | `Notification/…/NotificationSendController.cs` | 50 | `internal/notifications/send` | 2026-09-15 **已过期** |
- **原因**：内部路由统一加 `v1` 前缀的过渡期
- **影响**：旧路由全部仍在服务，攻击面与维护面扩大；`[Obsolete]` 仅编译期提示，HTTP 层无任何废弃告警/日志
- **是否需处理**：**是** —— 逐条确认调用方已迁移后物理删除旧路由（建议加临时反向代理日志确认零调用）

#### C3 — gRPC 契约 int64 + string 双轨字段（Guid 表达兼容）
- **位置**
  - `Order/…/GrpcServices/OrderGrpcService.cs:49`：优先读 `order_id_str`，回退 `order_id`（int64 X16 十六进制反序列化）
  - `Cart/…/Grpc/GrpcProductSnapshotBatchTests.cs:15`（int64+string 双轨字段）
  - 测试：`ReviewAfterSales …GetProductRating_LegacyClient_OnlyInt64_ThrowsInvalidArgument`、`SellerShop …GetShopInfo_LegacyClient_OnlyInt64_StillWorks`
- **原因**：proto 无 Guid 原生类型，过渡期同时下发 int64 与 string
- **影响**：客户端行为不一致 —— Review 域对旧客户端**抛错**，SellerShop 域对旧客户端**兼容**，语义不统一
- **是否需处理**：**待确认** —— 需确认是否仍有只发 int64 的旧客户端；若无则统一为 `*_str` 并移除 int64 字段

---

### D. 业务路径灰度开关双轨（Order / Promotion / Cart）

#### D1 — Order 下单 Saga 状态机 vs 进程内编排
- **位置**
  - 开关：`OrderSagaOptions.UseSagaStateMachine`（**默认 false**）+ `RolloutPercent`
  - `Order/…/Services/OrderSagaOrchestrator.cs:84, 152, 168-198, 229-236`（shadow 模式：旧编排仍执行，Saga 仅异步影子跑）
  - `Order/…/Sagas/OrderSagaStateMachine.cs` + `order_saga_states` 表
- **原因**：3.2 Saga 迁移，采用 shadow 双跑降低风险
- **影响**：**默认关闭 → `OrderSagaStateMachine` 及 `order_saga_states` 表在生产从未被激活**，
  但代码、EF 配置、Saga 仓储、迁移脚本均已就位；shipping 语义完全由旧路径承担
- **是否需处理**：**是** —— 明确决策：切流启用，或删除 Saga 相关代码与表

#### D2 — 支付后 Process Manager vs 旧内联路径
- **位置**
  - 开关：`OrderPaymentProcessOptions.UsePaymentProcessManager`（**默认 false**）+ `RolloutPercent`；`OrderPaymentProcessRolloutEvaluator.cs`
  - shadow 转发点：`PaymentSucceededEventConsumer.cs:59, 90, 105`、`StockConfirmConsumer.cs:105`、`PointsConfirmConsumer.cs:105`
  - `order_payment_processes` 表 + `ErCoreOrderPaymentProcessRepository`
- **原因**：3.3 Process Manager 迁移，shadow 双跑
- **影响**：同上 —— 三路并行编排（MarkOrderPaid / ConfirmStock / ConfirmPoints）在生产未激活；
  shadow 转发失败被刻意吞掉（"不影响旧路径"），可能长期无人察觉 Process Manager 是否真能正常工作
- **是否需处理**：**是**（同 D1，建议安排一次 flag=true 的端到端演练）

#### D3 — Promotion 规则引擎 vs 旧算法
- **位置**
  - 开关：`Promotion:UseRuleEngine`（**默认 false**，`PromotionOptions.cs`）
  - `PromotionCalculateAppService.cs:55-64`（flag 路由）、`:131-132` `CalculateLegacyAsync` `[Obsolete]`
  - 规则实现：`Rules/FullReductionRule.cs`、`Rules/CouponRule.cs`（注释声明"算法完整迁移自旧路径，向后兼容"）
- **原因**：规则引擎化重构，灰度对比
- **影响**：两套折扣算法并存 → 存在**结果不一致**风险（尤其边界用例）；默认走旧路径意味着规则引擎未经生产验证
- **是否需处理**：**是** —— 需完成灰度对比后择一删除

#### D4 — Cart SKU 快照 vs 实时调用
- **位置**
  - 开关：`Cart:UseSkuSnapshot`（**默认 false**）
  - `SnapshotCartPriceService`（新，装饰器）vs `GrpcCartPriceService.cs:21-24`（`[Obsolete]`，"降级备份与旧路径兼容"）
  - `CartItem.cs:56`：`UnitPriceSnapshot` 可空，"历史购物车项在回填前为 null，回退到实时调用路径"
- **原因**：3.11 快照化改造，需数据回填配合
- **影响**：回填脚本 `deploy/helm/leno/files/migrations/cart-sku-snapshot-backfill.sql` 存在，
  但 flag 仍为 false → **回填与读取开关未联动**；`UnitPriceSnapshot` 可空导致空值分支长期存在
- **是否需处理**：**是** —— 确认回填完成后开启 flag，并将 `UnitPriceSnapshot` 收敛为非空

---

### E. 查询与读模型双轨

#### E1 — CQRS QueryHandler vs 旧 AppService 方法
- **位置**
  | 文件 | 行 | 弃用声明 |
  |------|----|---------|
  | `Order/…/OrderAppService.cs` | 417, 425, 441 | 2026-08-01 **已过期** |
  | `Order/…/IOrderAppService.cs` | 52, 82, 93 | 2026-08-01 **已过期** |
  | `Product/…/SPUAppService.cs` | 186, 194 | 2026-08-01 **已过期** |
  | `Product/…/ISPUAppService.cs` | 31, 35 | 2026-08-01 **已过期** |
  | `SellerShop/…/SellerDashboardAppService.cs` | 31 | 2026-10-01 |
  | `SellerShop/…/ISellerDashboardAppService.cs` | 12 | 2026-10-01 |
  - 新：`Order/…/Queries/OrderDetailQuery(Handler).cs`、`LogisticsTraceQuery(Handler).cs`（注释："双发期 2 周内并存，2 周后 Controller 切换"）
- **原因**：CQRS 读侧重构，双发期并存
- **影响**：同一读能力两套实现；声明移除日期已过但方法仍被 Controller 使用 → **双发期未按计划收敛**
- **是否需处理**：**是** —— 优先完成 Order / Product 两处（已过期 1.5 个月）

#### E2 — SellerShop 读模型开关
- **位置**：`SellerShop:UseReadModel`（**默认 false**）；`ReadModelRebuilder` / ES 读模型
- **原因**：读模型灰度
- **影响**：`ShopDashboardQuery` Handler 读 ES，flag=false 时走 SQL；ES 读模型未经生产流量验证
- **是否需处理**：**是**

#### E3 — 读模型双存储（ES + SQL 快照）
- **位置**：`ReadModelSyncConsumerBase`（写 ES）与 `SqlSnapshotStore`（写 SQL 快照）双写；`ReadModelRebuilderTests`
- **原因**：ES 可用性兜底设计
- **影响**：需确认两者是否一致性校验；双写失败时以谁为准未明确
- **是否需处理**：**待确认**（可能是有意的降级设计）

---

### F. 持久化与基础设施双轨

#### F1 — Cart 私有 UnitOfWork（唯一非泛型实现）
- **位置**
  - `src/Services/Cart/Leno.Cart.Infrastructure/CartUnitOfWork.cs` —— 全仓**唯一** `class *UnitOfWork : IUnitOfWork`
  - 注册：`Cart/…/Dependencies/ServiceCollectionExtensions.cs:52` `services.AddScoped<IUnitOfWork, CartUnitOfWork>()`
  - 其它 18 个 BC 均使用共享 `EfCoreUnitOfWork<TDbContext>`
- **原因**：泛型 UoW 抽取（"消除约 680 行重复代码"）时漏掉 Cart
- **影响**：Cart 与其它 BC 行为可能漂移（尤其 Outbox 保存语义）；`CartUnitOfWork.SaveChangesAsync:40` 也带 `[Obsolete]` 标注 —— 与 `EfCoreUnitOfWork` 重复实现同一废弃约定
- **是否需处理**：**是** —— 迁移到 `EfCoreUnitOfWork<CartDbContext>` 并删除副本

#### F2 — `SaveChangesAsync` 废弃重载
- **位置**：`EfCoreUnitOfWork.cs:56-58`、`CartUnitOfWork.cs:40`（均 `[Obsolete]`，内部委托 `SaveChangesWithOutboxAsync`）
- **原因**：强制走 Outbox 的过渡保留
- **影响**：保留仅为兼容；若有调用方仍在用，`[Obsolete]` 仅编译期告警
- **是否需处理**：**待确认** —— 确认零调用后删除

#### F3 — Redis 匿名购物车旧格式 / 旧写方法
- **位置**
  - `RedisAnonymousCartRepository.cs:356` `SaveAsyncLegacy` `[Obsolete("1 个版本周期后删除")]`
  - `GetAsync` 兼容旧字符串格式（`RedisAnonymousCartRepositoryTests.cs:105` `GetAsync_LegacyStringFormat_ShouldLoadCartWithVersionZero`）
  - 认证路径用 SQL rowversion，匿名路径用 Redis `Revision` CAS（`Cart.cs:36`、`CartConfiguration.cs:27`）
- **原因**：匿名购物车 CAS 改造 + 存储格式升级
- **影响**：读取侧需长期保留旧格式解析分支；两套并发控制机制（rowversion / Revision）易混淆
- **是否需处理**：**是**（删除 `SaveAsyncLegacy`；旧格式解析需确认线上是否仍有存量键）

#### F4 — Outbox 热表 / 归档表
- **位置**：`outbox_messages` + `outbox_messages_archive`；`SystemAdmin/…/OutboxArchivalBackgroundService.cs`（每日 02:00 UTC，保留 7 天）
- **原因**：冷热分离设计
- **影响**：有意的架构设计，非缺陷
- **是否需处理**：**否**（有意保留）

#### F5 — 乐观锁两套机制
- **位置**
  - `BaseDbContext` 统一为继承 `Entity` 的实体注入 `version` shadow rowversion
  - `OrderConfiguration` 显式声明 `row_version` 列（基类逻辑会跳过显式声明者）
  - 迁移 `20260721999999_DropOrderVersionShadowColumn.cs`（曾删除 Orders 的 shadow `version`）
  - Cart 匿名路径用 Redis `Revision` CAS（无 rowversion）
- **原因**：历史迁移 + 存储介质差异（SQL vs Redis）
- **影响**：列名两套（`version` / `row_version`）；跨 BC 排查并发问题时需注意
- **是否需处理**：**待确认** —— 注释已说明是有意区分，建议仅统一命名规范

---

### G. 鉴权与配置双轨

#### G1 — 鉴权双模式 `Auth:Mode`
- **位置**：`WebApplicationExtensions.cs:122-153`（`JwtBearer` 分支 + `GatewayHeader` 分支 + `GatewayAuthHandler`）；19 个 BC `appsettings.json` 全部为 `"Mode": "JwtBearer"`
- **原因**：网关直连（GatewayHeader）与直连服务（JwtBearer）双部署形态
- **影响**：`GatewayHeader` 分支在生产从未启用 → 若网关改造为透传模式则未经验证
- **是否需处理**：**待确认** —— 属有意保留的能力，但需确认目标形态并安排验证

#### G2 — Identity 密钥托管双轨（**与项目"全本地自建"约束冲突**）
- **位置**：`Identity/…/appsettings.json`：`Identity:UseKmsForPepper = false`、`Identity:UseAzureKeyVault = false`
- **原因**：本地实现 vs 云 KMS/KeyVault 双实现
- **影响**：`UseAzureKeyVault` 引入 **Azure 云依赖**，与项目"不使用任何云服务、全本地自建"的架构约束**直接冲突**；若代码路径真实存在，则属于违规依赖
- **是否需处理**：**是（高优先级）** —— 建议移除 Azure KeyVault 路径及其配置项与依赖包，
  统一到本地 KMS/配置方案；如为历史残留应尽快清理

#### G3 — 文件存储双 Provider
- **位置**：`Leno.Infrastructure/Dependencies/ServiceCollectionExtensions.cs` `AddFileStorage`：`FileStorage:Provider` = `Local`（默认 `LocalFileStorageService`）vs `MinIO`（`ObjectStorageService`）
- **原因**：本地开发 vs 对象存储
- **影响**：两套实现仅在 `IFileStorageService` 接口下对齐，行为差异（URL 形态、权限）需关注
- **是否需处理**：**否**（有意保留的环境差异能力）

---

## 4. 优先级建议

### P0 — 影响生产正确性 / 架构约束
| 项 | 说明 |
|----|------|
| **B1** | 删除 `OutboxPublisher` 旧类与测试，避免误注册导致崩溃（本就无人注册，属死代码） |
| **G2** | 移除 `UseAzureKeyVault` 云依赖路径，回归"全本地自建"约束 |
| **C1** | 修正 AccessControl / Identity 的 `UseGrpc` 声明冲突（appsettings=true vs Consul KV=false） |
| **Outbox 部署** | 为多副本部署补充 `OUTBOX__SHARDING__SHARD_ID` / `__SHARD_COUNT`（本次修复的运维闭环） |

### P1 — 已过声明期，应尽快收敛
| 项 | 说明 |
|----|------|
| **C2** | 5 处内部路由旧前缀，4 处已过下线日期（2026-08-01 / 08-15 / 09-15） |
| **E1** | Order / Product 的旧 AppService 读方法，声明 2026-08-01 移除，已超期 |
| **B2** | 移除"双身份"死代码分支，简化 Outbox 保存逻辑 |
| **F1** | Cart 私有 `CartUnitOfWork` 迁移到共享泛型 UoW |

### P2 — 需明确决策（切流 or 删除）
| 项 | 说明 |
|----|------|
| **D1 / D2** | Order Saga 与 Process Manager 均默认关闭；建议安排一次 flag=true 端到端演练后再定去留 |
| **D3 / D4** | Promotion 规则引擎、Cart 快照 —— 需完成灰度对比 / 数据回填后收敛 |
| **A1 / A2** | PointsMembership、Review/AfterSales 拆分 —— 双轨期已过，需决策切流或回收代码 |
| **A3 / A4** | 8 个未部署 BC —— 列入部署就绪度清单 |

### P3 — 技术债 / 一致性
| 项 | 说明 |
|----|------|
| **A5** | compose 与 Helm 端口矩阵统一 |
| **B3 / B4** | 延迟消息语义与调度器插件确认 |
| **C3** | gRPC `*_str` / int64 双字段统一 |
| **E2 / E3** | SellerShop 读模型开关、ES/SQL 双写一致性 |
| **F2 / F3 / F5** | 废弃重载清理、Redis 旧格式清理、乐观锁列名统一 |
| **G1** | `GatewayHeader` 模式验证 |

---

## 5. 全量双轨清单（汇总表）

| ID | 双轨对象 | 位置 | 原因 | 需处理 |
|----|---------|------|------|--------|
| A1 | PointsMembership vs Points+Membership BC | `src/Services/PointsMembership/**`；`docker-compose.yml:349`；Helm `values.yaml:194` | BC 拆分（4.9），双轨期 8 周 | 是 |
| A2 | ReviewAfterSales vs Review+AfterSales | `docker-compose.yml:379`；`src/Services/Review/**`、`AfterSales/**` | BC 拆分双轨 | 是 |
| A3 | 19 个 BC 代码 vs 11 个已部署服务 | 各 `*.Api` 项目；`docker-compose.yml`；Helm `values.yaml:42` | 渐进式交付 | 是 |
| A4 | 19 个 DB 连接串 vs 12 个迁移脚本 | `kv-seed.json:5-23`；`deploy/helm/leno/files/migrations/` | 随 A3 | 是 |
| A5 | Compose 端口 vs Helm 端口 | `docker-compose.yml` vs `values.yaml` | 两套编排独立演进 | 是（低） |
| B1 | OutboxPublisher vs ShardedOutboxPublisher | `…/Outbox/OutboxPublisher.cs`、`ShardedOutboxPublisher.cs` | 4.4 分片替换 | 是 |
| B2 | 域事件双身份 vs IIntegrationEventMapper | `OutboxDbContextExtensions.cs` 两处回退分支 | 双发期兼容（已过期） | 是（死代码） |
| B3 | IEventBus 直发 vs Outbox 发布 | `RabbitMqEventBus.cs`；`OrderSagaOrchestrator.cs` | Outbox 迁移期 | 待确认 |
| B4 | MassTransit 调度器延迟消息 | `OrderSagaOrchestrator` `ScheduleSend`；`queue:order-timeout` | 延迟投递实现 | 待确认 |
| C1 | 防腐层 HTTP vs gRPC | `AntiCorruption:UseGrpc`；`*DispatcherAdapter.cs`；`kv-seed.json:47-57` | M4 灰度迁移 | 是 |
| C2 | 内部路由旧前缀 vs `internal/v1` | 5 个 Controller（Order/Product/Promotion/Points/Notification） | 统一 v1 前缀过渡 | 是 |
| C3 | gRPC int64 vs string Guid 字段 | `OrderGrpcService.cs:49`；相关 gRPC 测试 | proto 无 Guid 类型 | 待确认 |
| D1 | Order Saga vs 进程内编排 | `OrderSagaOptions.cs`；`OrderSagaOrchestrator.cs:84,152` | 3.2 Saga 迁移 | 是 |
| D2 | 支付后 Process Manager vs 内联路径 | `OrderPaymentProcessOptions`；3 个 Consumer 的 shadow 转发 | 3.3 迁移 | 是 |
| D3 | Promotion 规则引擎 vs 旧算法 | `Promotion:UseRuleEngine`；`PromotionCalculateAppService.cs:131` | 规则引擎化 | 是 |
| D4 | Cart SKU 快照 vs 实时调用 | `Cart:UseSkuSnapshot`；`GrpcCartPriceService.cs:24` | 3.11 快照化 | 是 |
| E1 | CQRS QueryHandler vs AppService 读方法 | Order/Product/SellerShop 的 `[Obsolete]` 方法 | CQRS 读侧重构 | 是 |
| E2 | SellerShop 读模型 ES vs SQL | `SellerShop:UseReadModel` | 读模型灰度 | 是 |
| E3 | 读模型 ES vs SQL 快照双写 | `ReadModelSyncConsumerBase`；`SqlSnapshotStore` | 兜底设计 | 待确认 |
| F1 | CartUnitOfWork vs EfCoreUnitOfWork | `CartUnitOfWork.cs`；Cart DI `:52` | 泛型 UoW 抽取遗漏 | 是 |
| F2 | `SaveChangesAsync` 废弃重载 | `EfCoreUnitOfWork.cs:56`；`CartUnitOfWork.cs:40` | Outbox 强制过渡 | 待确认 |
| F3 | Redis 匿名车旧格式 / SaveAsyncLegacy | `RedisAnonymousCartRepository.cs:356` | CAS 改造 + 格式升级 | 是 |
| F4 | outbox_messages vs archive 表 | `OutboxArchivalBackgroundService.cs` | 冷热分离（设计） | 否 |
| F5 | rowversion shadow vs 显式列 / Redis Revision | `BaseDbContext.cs:94-109`；`OrderConfiguration`；`Cart.cs:36` | 历史迁移 + 介质差异 | 待确认 |
| G1 | JwtBearer vs GatewayHeader | `WebApplicationExtensions.cs:122` | 双部署形态 | 待确认 |
| G2 | Identity 本地 vs Azure KeyVault/KMS | `Identity appsettings.json:62,74` | 密钥托管双轨 | 是（冲突） |
| G3 | FileStorage Local vs MinIO | `ServiceCollectionExtensions.AddFileStorage` | 环境差异 | 否 |

**统计**：需处理 17 项 / 待确认 8 项 / 有意保留 2 项。

---

## 6. 附录：本次修复的验证证据

```
dotnet build Leno.slnx
  Build succeeded.
    763 Warning(s)   ← 全部为既有历史告警（LENO_PM_BC_SPLIT 等），与本次改动无关
      0 Error(s)

dotnet test Leno.Infrastructure.Tests --filter "FullyQualifiedName~Outbox"
  Total tests: 63
       Passed: 63
```
