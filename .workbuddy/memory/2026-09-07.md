# 2026-09-07 工作日志

## 部署就绪度评审
- 团队 software-leno-deploy-review：架构师完成全项目部署就绪度评审，评分 55/100。
- 4 项 P0：① Helm 注入 ConnectionStrings__Default 与代码读取键名 {Bc}Db 不匹配；② migration-job 用 aspnet 运行时镜像跑 dotnet-ef 必失败（应改用 scripts/migrations 幂等 SQL）；③ 5 个生产 Secret 无创建渠道、secret.yaml 为 REPLACE_ME 占位；④ 生产基础设施（SQL Server/Redis/RabbitMQ/Consul）零方案，Consul 为网关服务发现单点。
- 关键事实：12 个部署单元 vs 19 个服务工程（8 个无 Dockerfile）；网关灰度路由指向不会有实例的服务名；12 个 appsettings.Docker.json 明文密码已入库需轮换；CI 只 build 不 push。
- 完整报告：D:/work/learn/Leno/.workbuddy/reports/deployment-readiness-2026-09-07.md

## 用户决策确认（13:14）
- Consul KV 确认规划为生产配置主通道 → P0-1 修复方向改为：KV 覆盖面审计 + 种子化方案 + deployment.yaml 仅保留最小引导 env（Consul__Address/JWT/OTel）+ 启动 fail-fast 校验。
- Inventory 未入网关路由确认为遗漏 → 需补网关路由/集群定义，并纳入 8 服务容器化补齐清单。
- 评审报告已同步更新：D:/work/learn/Leno/.workbuddy/reports/deployment-readiness-2026-09-07.md

## P0 修复完成（QA GO）
- 团队 software-leno-p0-fixes：工程师 8 个 commit（c8a112d3…3ee87c7b，dev 分支）修复 4 组 P0，QA 独立回归 8/8 通过，结论 GO。
- 关键事实修正：代码读取 Consul:Url（非 Address）、Jwt:SecretKey（GetSection("Jwt")）、连接串 key={Bc}Db（ReviewAfterSalesDb 特例）；Consul KV 优先级高于 env 兜底。
- 新增：deploy/docs/consul-kv-coverage-audit.md、deploy/consul/kv-seed.json、deploy/scripts/{seed-consul-kv,create-secrets}.ps1、deploy/helm/leno/templates/migrations-configmap.yaml、files/migrations/ 14 SQL、StartupConfigurationValidation.cs、网关 Inventory 路由。
- 遗留技术债：① CI 需补 scripts/migrations→chart files 同步步骤；② staging/prod 需 ESO 已配置；③ 既有 24 个环境依赖类测试失败（ObjectStorage/UnitOfWork 等与本次无关）；④ 网关 appsettings Jwt:SecretKey 占位待 KV 覆盖；⑤ 本机 .git 曾对象库损坏，注意文件同步工具干扰 .git/objects/pack。

## 阶段 2/3 完成（QA GO）
- 团队 software-leno-stage23：架构师产出 deploy/docs/production-infrastructure-plan.md（推荐：SQL Server 云托管 RDS 2 实例、Redis Sentinel、RabbitMQ 3.13 Quorum、Consul consul-k8s 3 节点+ACL、ES 自建 3 节点；D1-D7 待用户决策）；工程师 4 commits（ec7bcf73/d224c867/374f8ca3/8f65c77d）：CI 镜像 push GHCR（tag=sha+分支，main+latest）、migration-files-sync 双向 diff 检查、镜像参数化 global.imageRegistry/Namespace/Tag（服务级可覆盖）、cd.yml 骨架（workflow_dispatch + --atomic + rollout status）。
- 关键决策：服务镜像统一命名 leno-<service>（CI push == CD verify == helm repository 三方一致）；GHCR owner 由 --set global.imageNamespace 注入。
- QA 两轮：第 1 轮文档 6 处事实错误（Redis 用途漏库存/秒杀等业务关键层、ESO 无 Consul provider、MassTransit 实为 fanout 非 Topic、SQL 分组漏 aftersales 等）→ 架构师 0d358c3 修复 → 第 2 轮 GO。
- 镜像名统一 leno-<x> 后 docs/handbook/09-deployment-and-ops.md 示例未同步（非阻塞技术债）。
- GitHub 需配置：Environments staging/prod（prod 加 required reviewers）、环境级 KUBECONFIG_STAGING/KUBECONFIG_PROD（base64）、GHCR 私有时集群 imagePullSecret。

## 用户决策：全本地自建（15:53）
- 不采用任何云服务（无云 RDS/云托管中间件/云日志/云备份），环境全部本地搭建。
- 影响：D1/D2/D3/D6/D7 决策项重做，SQL Server 选型从"云托管 RDS"改为本地自建（VM/容器化待架构师重推荐）；备份目标改本地 NAS/离线介质。
- 团队 software-leno-onprem-rev 已派架构师增量修订 production-infrastructure-plan.md，QA 待验证。

## 全本地自建方案定稿（QA GO，v1.2）
- 团队 software-leno-onprem-rev：f2083c0e 按"无云服务"约束修订 → QA 第 1 轮 NO-GO（Basic AG 每 AG 仅 1 库的微软官方限制未提及）→ dba0d517 修复（19 个 Basic AG + cluster type NONE + 脚本化整实例切换，a1 默认；a2 Enterprise 单 AG；a3 FCI）→ 第 2 轮 GO。
- SQL Server 最终推荐：独立 Linux VM×2 + Standard Per Core + 19 Basic AG（cluster type NONE，无 Pacemaker，手动/脚本切换，RPO≈0）；staging 用 K8s 单节点 Developer 版；备份本地 NAS bk-01 + 离线介质（3-2-1）；日志自建 Loki；RTO ≤60 分钟。
- Known Issue（措辞级）：L60 FAILOVER 命令名应为 FORCE_FAILOVER_ALLOW_DATA_LOSS 流程（下版顺手修）。
- 待用户拍板 D1-D7（v1.2 §6）。Edit 工具本机存在偶发"假成功"，重要编辑后需 Read/Grep 验证落盘。

## 用户确认 D1-D7 按默认建议（17:56，commit c0359ebd）
- 方案 v1.2 定稿生效：D1=a1（主备 VM+19 Basic AG+cluster type NONE）、D2=生产独立 VM/staging K8s Developer、D3=中档、D4=RabbitMQ 3.13、D5=目标态自建 Vault/过渡态脚本、D6=staging K8s 单节点、D7=自建 Loki。
- 下一阶段：阶段 4 staging 物料准备与端到端部署演练（缺口：consul-staging-values.yaml 未落盘、deploy/staging/ 不存在、staging SQL Server K8s 清单缺失）。

## 阶段 4 staging 物料完成（QA GO）
- 团队 software-leno-stage4-staging：工程师 5 commits（aa9cd206/a8d35ea7/5ceb77aa/ab09e055 + 修复 be1c9d48）补齐 staging 物料；QA 两轮（第 1 轮 NO-GO：consul 资源名需双段 consul-consul-server、ServiceUrl leno-userauth、Consul Token 403 风险；修复后第 2 轮 GO，三环境渲染实测 12/12 Deployment 注入 leno-consul-token:acl-token）。
- 新物料：deploy/helm/consul-staging-values.yaml、deploy/staging/{mssql-staging.yaml,loki-staging-values.yaml,README.md（核心 Runbook）}、.env.example 补 19 库变量+LENO_CONSUL_TOKEN。
- 关键机制：Consul__Token 参数化注入（tokenSecret.name 空=不注入；staging/prod=leno-consul-token，dev 关闭）；staging 过渡态手工 Secret 四件套（leno-security-jwt/leno-staging-mssql/leno-staging-grafana-admin/leno-consul-token）。
- 生产前必办遗留：①leno-security-jwt 自动化；②externalSecrets 改 vault backend+ESO 清单；④leno_app 独立密码；⑤cd.yml namespace leno-staging vs Runbook leno 对齐；⑥Loki prod 形态重评。
- staging 实际搭建需真实机器，按 deploy/staging/README.md 步骤 0-7 执行。

## 推送 dev 与 CI 首跑（2026-09-08 09:34）
- git push origin dev 成功：89559c01..be1c9d48（fast-forward 验证后推送；网络不稳定需 -c http.postBuffer=524288000 重试）。
- 远端仓库 github.com/junjie-git/leno（公开可见，Actions 页可匿名访问）。
- CI run #6（ci.yml，be1c9d48）已触发排队；上一次 run #5（89559c01）成功 4m22s。本轮首次包含 GHCR push（packages:write）与 migration-files-sync 检查。
- 待观察：CI 全量结果（含"干净环境 0 错误"验证、GHCR 推送是否成功）。

## CI 修复完成待推送（2026-09-08 上午）
- 重要根因修正：run #5（89559c01）本身就是 failure——build-solution/web-system-admin/migration-check 三个 job 在 #5 已红且失败原因相同，4 个 CI 失败均为存量问题 + runner 移除 docker-compose v1，与本轮 deploy 物料提交无关；我们的 docker-build 反而修绿了 #5 全红的构建。
- 工程师 4 commits（be1c9d48..4dd8f72a，本地待推送）：4fdd17ce fix(ci)（compose v2 语法 + migration-check 注入设计期连接串）；e8618bb8 fix(code)（check-placeholders 拦截项：Assert.True(true) 与 TODO 注释）；352332cb fix(code)（CA1001/CS8602/CA1805/CA1859/CA2263 逐处修复，9 文件）；4dd8f72a fix(web)（system-admin 6 error + 20 warning 清零）。未动全局分析器配置。
- 本地验证：dotnet build Leno.slnx exit 0；pnpm lint/typecheck/test 全过（234 用例）。
- ⚠️ 推送被网络阻塞（schannel 中断/代理 502，重试 4 次未果）。待网络恢复后：git push origin dev → CI run #7 → QA 验证全绿（任务 #11）。
- 本机工具注意：dotnet CLI 需补 ProgramFiles(x86)/ProgramData 环境变量（便携 SDK 在 .workbuddy/dotnet-sdk）；pnpm shim 损坏需 corepack 直调。
